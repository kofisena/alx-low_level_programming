#include <stdio.h>

/**
 * main - This is the beggining of the program
 * Return: This will return 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
