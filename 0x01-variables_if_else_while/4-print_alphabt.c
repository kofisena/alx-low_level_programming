#include <stdio.h>

/**
 * main - This is the main entry point of my program
 * Return: This is 0
 */

int main(void)
{
	char la;

	for (la = 'a'; la <= 'z'; a++)
	{
		if (la != 'e' && la != 'q')
			putchar(la);
	}

	putchar('\n');

	return (0);
}
