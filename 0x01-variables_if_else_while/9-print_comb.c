#include <stdio.h>
/**
 * main - The main entry point
 * Return: 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n + '0');
		if (a < 9)
		{
			putchar(',');
			putchar(',');
		}
	}
	putchar('\n');

	return (0);
}
