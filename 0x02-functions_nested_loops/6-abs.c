#include "main.h"

/*
 * _abs - calculates the abs value of an int
 * @n: the parameter
 *
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n <= 0)
		return (n);
	else
		return (-n);
}
