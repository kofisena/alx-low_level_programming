#include "main.h"

/**
 * factorial - outpus te factorial of a number
 * @n: The number for the factorial
 * Return: output
 */

int factorial(int n)
{
	int result = n;

	if (n < 0)
		return (-1);
	else if (n >= 0 && n <= 1)
		return (1);
}
